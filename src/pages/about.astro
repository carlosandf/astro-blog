---
import BaseLayout from "../layouts/BaseLayout.astro";
const pageTitle = "Sobre mi";

const identity = {
  firstName: "Carlos",
  country: "Colombia",
  occupation: "Frontent Developer",
  hobbies: ["fotografia", "Deportes", "Musica"],
};

const skills = [
  "HTML",
  "CSS",
  "JavaScript",
  "React",
  "Astro",
  "Redacción de documentos",
];

const happy = true;
const finished = false;
const goal = 3;

const vars = {
  skillColor: "navy",
  fontWeight: "bold",
  textCase: "uppercase",
};
---

<BaseLayout title={pageTitle}>
  <div class="main-conteiner">
    <section class="presentation">
      <h1 class="title">{pageTitle}</h1>
      <h2 class="subtitle">... ¡y mi nuevo sitio Astro!</h2>

      <div class="content">
        <p>
          Estoy trabajando en el tutorial introductorio de Astro. Esta es la
          segunda página de mi sitio web, ¡y es la primera que he construido yo
          mismo!
        </p>

        <p>
          Este sitio se irá actualizando a medida que vaya completando más
          partes del tutorial, ¡así que no dejes de visitarlo para ver cómo va
          mi viaje!
        </p>
      </div>
    </section>
    <section class="about-me">
      <h3>He aquí algunos datos sobre mí:</h3>

      <ul>
        <li>Me llamo {identity.firstName}</li>
        <li>Vivo en {identity.country} y trabajo como {identity.occupation}</li>
        {
          identity.hobbies.length >= 2 && (
            <li>
              Mis aficiones son:
              <ul>
                {identity.hobbies.map((hobbie) => (
                  <li>{hobbie}</li>
                ))}
              </ul>
            </li>
          )
        }
      </ul>
      <h3>Mis habilidades:</h3>
      <ul>
        {skills.map((skill) => <li class="skill">{skill}</li>)}
      </ul>
    </section>
    <section>
      {happy && <p>¡Estoy feliz de aprender Astro!</p>}

      {finished && <p>¡He terminado este tutorial!</p>}

      {
        goal === 3 ? (
          <p>Mi objetivo es terminar en 3 días.</p>
        ) : (
          <p>Mi objetivo no son 3 días.</p>
        )
      }
    </section>
  </div>
</BaseLayout>

<style define:vars={vars}>
  .main-conteiner {
    display: grid;
    gap: 4rem;
  }

  /* PRESENTATION */
  .presentation .title {
    color: purple;
    font-size: 5rem;
  }
  .presentation .subtitle {
    font-size: 2rem;
  }
  .presentation .content > p {
    font-size: 1.4rem;
  }

  /* ABOUT ME */

  .about-me h3 {
    font-size: 1.8rem;
  }
  .about-me ul {
    font-size: 1.5rem;
    padding-inline-start: 4rem;
  }

  .skill {
    color: var(--skillColor);
    font-weight: var(--fontWeight);
    text-transform: var(--textCase);
  }
</style>
